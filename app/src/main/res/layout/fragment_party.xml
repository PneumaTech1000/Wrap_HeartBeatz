<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/party_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorSurface">

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/partySnackbar"
        style="@style/Widget.Material3.CardView.Elevated"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:visibility="gone"
        app:cardBackgroundColor="?attr/colorSurfaceInverse"
        app:cardCornerRadius="16dp"
        app:cardElevation="6dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:strokeWidth="0dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:paddingHorizontal="16dp"
            android:paddingVertical="12dp"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/tvPartySnackbarMessage"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textColor="?attr/colorOnSurfaceInverse"
                android:textAppearance="@style/TextAppearance.Material3.BodyMedium"
                android:text="" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnPartySnackbarAction"
                style="@style/Widget.Material3.Button.TextButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:visibility="gone"
                android:text="@string/retry"
                android:textColor="?attr/colorPrimaryInverse" />
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- WELCOME / IDLE PAGE -->
    <include
        android:id="@+id/welcome_page"
        layout="@layout/party_welcome_layout" />

    <!-- SEARCHING / CREATING PAGE -->
    <include
        android:id="@+id/searching_page"
        layout="@layout/party_join_pulse_layout"
        android:visibility="gone"/>

    <!-- SETUP REQUIRED PAGE -->
    <include
        android:id="@+id/setup_required_page"
        layout="@layout/party_setup_required_layout"
        android:visibility="gone"/>

    <!-- CONNECTED GUESTS LIST (For Host or Guest) -->
    <include
        android:id="@+id/connected_party"
        layout="@layout/party_connected_guest_layout"
        android:visibility="gone"/>

    <!-- NOW PLAYING CARD -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/cardNowPlaying"
        style="@style/Widget.Material3.CardView.Elevated"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="24dp"
        android:visibility="gone"
        app:cardCornerRadius="32dp"
        app:cardElevation="4dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:strokeWidth="0dp">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="24dp">

            <com.google.android.material.imageview.ShapeableImageView
                android:id="@+id/ivPartyAlbumArt"
                android:layout_width="120dp"
                android:layout_height="120dp"
                android:scaleType="centerCrop"
                android:src="@drawable/celebration_24px"
                app:shapeAppearanceOverlay="@style/ShapeAppearance.Material3.Corner.Large"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:strokeColor="?attr/colorOutlineVariant"
                app:strokeWidth="1dp" />

            <TextView
                android:id="@+id/tvPartySongTitle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="20dp"
                android:text="@string/streaming_audio"
                android:textAppearance="@style/TextAppearance.Material3.TitleLarge"
                android:textStyle="bold"
                android:ellipsize="marquee"
                android:singleLine="true"
                app:layout_constraintStart_toEndOf="@id/ivPartyAlbumArt"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="@id/ivPartyAlbumArt"
                app:layout_constraintBottom_toTopOf="@+id/tvPartyArtist"
                app:layout_constraintVertical_chainStyle="packed"/>

            <TextView
                android:id="@+id/tvPartyArtist"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="20dp"
                android:layout_marginTop="4dp"
                android:text="@string/syncing_with_host"
                android:textAppearance="@style/TextAppearance.Material3.BodyMedium"
                android:alpha="0.7"
                android:ellipsize="end"
                android:maxLines="1"
                app:layout_constraintStart_toEndOf="@id/ivPartyAlbumArt"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toBottomOf="@id/tvPartySongTitle"
                app:layout_constraintBottom_toBottomOf="@id/ivPartyAlbumArt"/>

            <com.google.android.material.progressindicator.LinearProgressIndicator
                android:id="@+id/progressSync"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="24dp"
                android:indeterminate="true"
                app:trackThickness="8dp"
                app:trackCornerRadius="4dp"
                app:layout_constraintTop_toBottomOf="@id/ivPartyAlbumArt" />

            <TextView
                android:id="@+id/tvTrackProgress"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:text="0:00 / 0:00"
                android:textAppearance="@style/TextAppearance.Material3.LabelMedium"
                android:textColor="?attr/colorOnSurfaceVariant"
                app:layout_constraintTop_toBottomOf="@id/progressSync"
                app:layout_constraintStart_toStartOf="parent" />

        </androidx.constraintlayout.widget.ConstraintLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- DISCOVERY OVERLAY -->
    <include
        android:id="@+id/found_party"
        layout="@layout/party_found_item_float"
        android:visibility="gone"/>

</androidx.constraintlayout.widget.ConstraintLayout>
