<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="false">

    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:id="@+id/root_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/root_container_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <!-- Fixed mini player ABOVE bottom nav (not BottomSheet peek — that was off-screen) -->
        <FrameLayout
            android:id="@+id/mini_player_host"
            android:layout_width="match_parent"
            android:layout_height="@dimen/media_player_bar_height"
            android:layout_gravity="bottom"
            android:layout_marginBottom="@dimen/navigation_bar_height"
            android:elevation="12dp"
            android:translationZ="12dp"
            android:visibility="gone"
            android:clickable="true"
            android:focusable="true" />

        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/root_navigation_bar"
            android:layout_width="match_parent"
            android:layout_height="@dimen/navigation_bar_height"
            android:layout_gravity="bottom"
            android:background="?attr/colorSurface"
            android:elevation="8dp"
            app:labelVisibilityMode="labeled"
            app:menu="@menu/bottom_nav_menu"
            app:itemIconTint="@drawable/bottom_nav_item_color"
            app:itemTextColor="@drawable/bottom_nav_item_color"
            app:itemRippleColor="?attr/colorPrimary"
            app:itemActiveIndicatorStyle="@style/Widget.HeartBeatz.BottomNav.ActiveIndicator" />

        <!-- Full player overlay (shown when expanded) -->
        <FrameLayout
            android:id="@+id/full_player_host"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:elevation="24dp"
            android:translationZ="24dp"
            android:visibility="gone"
            android:clickable="true"
            android:focusable="true"
            android:background="?attr/colorSurface" />

    </androidx.coordinatorlayout.widget.CoordinatorLayout>

    <include layout="@layout/drawer_side_panel" />

</androidx.drawerlayout.widget.DrawerLayout>
