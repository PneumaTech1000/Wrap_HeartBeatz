<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="false">

    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:id="@+id/root_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Primary content (Home / Party via show-hide) -->
        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/root_container_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginBottom="0dp" />

        <!-- Material 3 bottom navigation — fixed chrome, not a sliding panel -->
        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/root_navigation_bar"
            android:layout_width="match_parent"
            android:layout_height="@dimen/navigation_bar_height"
            android:layout_gravity="bottom"
            android:background="?attr/colorSurface"
            android:elevation="8dp"
            app:labelVisibilityMode="labeled"
            app:menu="@menu/bottom_nav_menu"
            app:itemIconTint="@drawable/bottom_nav_item_color"
            app:itemTextColor="@drawable/bottom_nav_item_color"
            app:itemRippleColor="?attr/colorPrimary"
            app:itemActiveIndicatorStyle="@style/Widget.HeartBeatz.BottomNav.ActiveIndicator" />

        <!-- Player only: Material bottom sheet (mini peek ↔ full screen) -->
        <!-- marginBottom = nav height so collapsed mini sits *above* bottom nav -->
        <FrameLayout
            android:id="@+id/player_bottom_sheet"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginBottom="@dimen/navigation_bar_height"
            android:elevation="16dp"
            android:translationZ="16dp"
            android:clickable="true"
            android:focusable="true"
            android:background="@android:color/transparent"
            app:layout_behavior="com.google.android.material.bottomsheet.BottomSheetBehavior"
            app:behavior_hideable="true"
            app:behavior_skipCollapsed="false"
            app:behavior_peekHeight="@dimen/media_player_bar_height"
            app:behavior_fitToContents="false"
            app:behavior_expandedOffset="0dp" />

    </androidx.coordinatorlayout.widget.CoordinatorLayout>

    <include layout="@layout/drawer_side_panel" />

</androidx.drawerlayout.widget.DrawerLayout>
